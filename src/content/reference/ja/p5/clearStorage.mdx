---
title: clearStorage
module: Data
submodule: LocalStorage
file: src/data/local_storage.js
description: >
  <p>ウェブブラウザのローカルストレージにあるすべてのアイテムを削除します。</p>

  <p>ウェブブラウザは組み込みの<a
  href="https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage/"
  target="_blank">localStorage オブジェクト</a>を使用して少量のデータを保存できます。

  <code>localStorage</code>に保存されたデータは、ページの更新やブラウザの再起動後でも、

  いつでも取り出すことができます。データはキーと値のペアとして保存されます。

  <code>clearStorage()</code>を呼び出すと、<code>localStorage</code>からすべてのデータが

  削除されます。</p>

  <p>注意：パスワードや個人情報などの機密データは<code>localStorage</code>に

  保存すべきではありません。</p>
line: 313
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    // ダブルクリックでlocalStorageをクリアします。

    function setup() {
      createCanvas(100, 100);

      // プレイヤーの名前を保存します。
      storeItem('name', 'Feist');

      // プレイヤーのスコアを保存します。
      storeItem('score', 1234);

      describe(
        'グレーの背景に黒で"Feist: 1234"というテキストが書かれています。ユーザーがダブルクリックすると"null: null"というテキストが表示されます。'
      );
    }

    function draw() {
      background(200);

      // テキストのスタイルを設定します。
      textAlign(CENTER, CENTER);
      textSize(14);

      // 名前を取得します。
      let name = getItem('name');

      // スコアを取得します。
      let score = getItem('score');

      // スコアを表示します。
      text(`${name}: ${score}`, 50, 50);
    }

    // ユーザーがダブルクリックしたときにlocalStorageをクリアします。
    function doubleClicked() {
      clearStorage();
    }
    </code>
    </div>
class: p5
chainable: false
---


# clearStorage