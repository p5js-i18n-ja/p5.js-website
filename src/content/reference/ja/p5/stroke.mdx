---
title: stroke
module: Color
submodule: Setting
file: src/color/setting.js
description: >
  <p>図形の輪郭の色を設定します。</p>

  <p><code>stroke()</code>は、その後に描画されるすべての図形の輪郭を

  指定された色、グラデーション、またはパターンで描画します。</p>

  <p>このメソッドは、さまざまな方法で呼び出すことができます：</p>

  <ul>

  <li><code>stroke(color)</code>：グレースケール値、カラーオブジェクト、または文字列で色を指定</li>

  <li><code>stroke(colorArray)</code>：色の値を含む配列で色を指定</li>

  <li><code>stroke(v1, v2, v3)</code>：RGB値またはHSB値で色を指定</li>

  <li><code>stroke(v1, v2, v3, alpha)</code>：RGB値またはHSB値とアルファ値で色を指定</li>

  <li><code>stroke(gray, [alpha])</code>：グレースケール値とオプションのアルファ値で色を指定</li>

  </ul>

  <p>色の解釈方法は、現在の<a href="/reference/p5/colorMode">colorMode()</a>に依存します。</p>

  <p><code>stroke()</code>を呼び出さない場合、図形はデフォルトで黒の輪郭で描画されます。</p>
line: '1737'
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
      background(200);

      // 赤い輪郭の正方形
      stroke(255, 0, 0);
      square(20, 20, 60);

      describe('赤い輪郭の正方形');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
      background(200);

      // 半透明の青い輪郭の正方形
      stroke(0, 0, 255, 127);
      square(20, 20, 60);

      describe('半透明の青い輪郭の正方形');
    }
    </code>
    </div>

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
      background(200);
      colorMode(HSB, 360, 100, 100);

      // HSBカラーモードで輪郭を設定
      stroke(180, 100, 100);
      square(20, 20, 60);

      describe('シアン色の輪郭の正方形');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 1373
    params:
      - name: v1
        description: >
          <p>RGBモードなら赤、HSBモードなら色相の値。</p>
        type: Number
      - name: v2
        description: >
          <p>RGBモードなら緑、HSBモードなら彩度の値。</p>
        type: Number
      - name: v3
        description: >
          <p>RGBモードなら青、HSBモードなら明度の値。</p>
        type: Number
      - name: alpha
        description: >
          <p>アルファ値（不透明度）。0は透明、255は不透明。</p>
        type: Number
        optional: true
    chainable: 1
  - line: 1596
    params:
      - name: value
        description: |
          <p>色を表す文字列。</p>
        type: String
    chainable: 1
  - line: 1602
    params:
      - name: gray
        description: |
          <p>グレー値。</p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
    chainable: 1
  - line: 1609
    params:
      - name: values
        description: |
          <p>色の赤、緑、青、およびアルファコンポーネントを含む配列。</p>
        type: 'Number[]'
    chainable: 1
  - line: 1616
    params:
      - name: color
        description: |
          <p>p5.Colorオブジェクト。</p>
        type: p5.Color
    chainable: 1
chainable: true
---

# stroke