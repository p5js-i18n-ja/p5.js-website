---
title: saveCanvas
module: Image
submodule: Image
file: src/image/image.js
description: >
  <p>キャンバスを画像として保存します。</p>

  <p>デフォルトでは、<code>saveCanvas()</code>はキャンバスをPNGファイルとして保存し、

  ファイル名を<code>untitled.png</code>とします。</p>

  <p>最初のパラメータ<code>filename</code>は、ファイル名を設定します。

  ファイル名には拡張子を含めないでください。例えば、<code>'myCanvas'</code>と

  指定すると、<code>'myCanvas.png'</code>として保存されます。</p>

  <p>2番目のパラメータ<code>extension</code>は、ファイルタイプを設定します。

  <code>'png'</code>または<code>'jpg'</code>を使用できます。</p>

  <p>3番目のパラメータ<code>canvas</code>は、保存するキャンバスを指定します。

  デフォルトでは、現在のキャンバスが使用されます。</p>

  <p>注意：ブラウザによっては、大きなキャンバスの保存に失敗する場合があります。</p>
line: 1
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    function setup() {
      createCanvas(100, 100);
      background(255);
      rect(20, 20, 60, 60);
      describe('白い背景に黒い正方形が描かれています。');
    }

    // キャンバスを保存するには、's'キーを押してください。
    function keyPressed() {
      if (key === 's') {
        saveCanvas('myCanvas', 'png');
      }
    }
    </code>
    </div>

    <div>
    <code>
    let cnv;

    function setup() {
      background(255);
      cnv = createCanvas(100, 100);
      rect(20, 20, 60, 60);
      describe('白い背景に黒い正方形が描かれています。');
    }

    // キャンバスを保存するには、's'キーを押してください。
    function keyPressed() {
      if (key === 's') {
        saveCanvas(cnv, 'myCanvas', 'jpg');
      }
    }
    </code>
    </div>
class: p5
overloads:
  - line: 153
    params:
      - name: selectedCanvas
        description: |
          <p>特定のHTML5キャンバス要素への参照。</p>
        type: p5.Framebuffer|p5.Element|HTMLCanvasElement
      - name: filename
        description: |
          <p>ファイル名。デフォルトは'untitled'。</p>
        type: String
        optional: true
      - name: extension
        description: >
          <p>ファイル拡張子、'png'、'webp'、または'jpg'。デフォルトは'png'。</p>
        type: String
        optional: true
  - line: 267
    params:
      - name: filename
        description: ''
        type: String
        optional: true
      - name: extension
        description: ''
        type: String
        optional: true
chainable: false
---

# saveCanvas
