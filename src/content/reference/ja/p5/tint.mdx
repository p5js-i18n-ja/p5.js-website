---
title: tint
module: Image
submodule: Loading & Displaying
file: src/image/loading_displaying.js
description: >
  <p>画像に色合いを適用します。</p>

  <p><code>tint()</code>は、後続のすべての<code>image()</code>呼び出しに影響します。
  画像を元の色で表示するには、<a href="/reference/p5/noTint">noTint()</a>を使用します。</p>

  <p>パラメータは<a href="/reference/p5/fill">fill()</a>と同じように指定します。</p>

  <p>画像の透明度を変更するには、オプションのアルファパラメータを使用するか、
  <code>tint()</code>の代わりに<a href="/reference/p5/image">image()</a>の
  オプションのアルファパラメータを使用します。</p>
line: 452
isConstructor: false
itemtype: method
example:
  - |-

    <div>
    <code>
    let img;

    // 画像を読み込みます。
    function preload() {
      img = loadImage('/assets/laDefense.jpg');
    }

    function setup() {
      createCanvas(100, 100);

      image(img, 0, 0);
      tint(0, 153, 204);  // 青色の色合い
      image(img, 50, 0);

      describe('2つの画像。左側は元の画像で、右側は青色の色合いがかかっています。');
    }
    </code>
    </div>

    <div>
    <code>
    let img;

    // 画像を読み込みます。
    function preload() {
      img = loadImage('/assets/laDefense.jpg');
    }

    function setup() {
      createCanvas(100, 100);
    }

    function draw() {
      image(img, 0, 0);
      tint(255, 126);  // 50%の透明度を適用
      image(img, 50, 0);

      describe('2つの画像。左側は元の画像で、右側は50%透明になっています。');
    }
    </code>
    </div>
class: p5
overloads:
  - line: 1192
    params:
      - name: v1
        description: |
          <p>グレースケール値、赤または色相の値（モードに応じて）。</p>
        type: Number
      - name: v2
        description: |
          <p>緑または彩度の値（モードに応じて）。</p>
        type: Number
      - name: v3
        description: |
          <p>青または明度の値（モードに応じて）。</p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
  - line: 1318
    params:
      - name: value
        description: |
          <p>色文字列。</p>
        type: String
  - line: 1323
    params:
      - name: gray
        description: |
          <p>グレースケール値。</p>
        type: Number
      - name: alpha
        description: ''
        type: Number
        optional: true
  - line: 1329
    params:
      - name: values
        description: |
          <p>色の赤、緑、青、およびアルファコンポーネントを含む配列。</p>
        type: 'Number[]'
  - line: 1335
    params:
      - name: color
        description: |
          <p>色オブジェクト。</p>
        type: p5.Color
chainable: false
---

# tint